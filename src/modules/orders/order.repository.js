import prismaClient from "../../config/db.js";
import logger from "../../utils/logger.js";

/**
 * Order repository – raw DB access layer.
 * Add methods here as the Order model is added to schema.prisma.
 */

export {};
